/**
 * @deprecated Temporary compatibility shim — source of truth moved to `dom-observer/`.
 * @see {@link ../../../dom-observer/DomPositionIndex.ts}
 * Remove this shim in a later cleanup PR.
 */
export { DomPositionIndex, type DomPositionIndexEntry } from '../../../dom-observer/DomPositionIndex.js';
